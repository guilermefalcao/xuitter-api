package br.com.xuitter.xuitter_api.entities;

// Enum que define os tipos de Xuit (post)
public enum XuitType {
    ORIGINAL,  // Post original
    REXUIT,    // Repost (como retweet)
    QUOTE      // Quote (repost com comentário)
}
